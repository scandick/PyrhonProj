# 📘 Полная инструкция по настройке Git, GitHub и командной работы

Эта инструкция описывает весь процесс: от установки Git до подключения участников, настройки веток и создания Pull Request. Используется для учебных проектов и подходит команде из 3–6 человек.

---

## 1. Установка Git и первичная конфигурация

### 1.1. Установка
Скачайте Git:
https://git-scm.com/downloads

Установите стандартными настройками.

### 1.2. Настройка имени и email
Откройте **Git Bash**:

```bash
git config --global user.name "Ваше имя"
git config --global user.email "email_на_GitHub"
Проверьте:

bash
Копировать код
git config --global --list
Эти данные будут отображаться в коммитах.

2. Создание репозитория на GitHub
Зайдите на GitHub → New repository

Укажите:

Repository name

Public или Private

При необходимости установите:

Add README (удобно)

Add .gitignore (Python, Node или другое)

Нажмите Create repository

3. Добавление участников (коллабораторов)
Откройте репозиторий на GitHub

Перейдите в Settings

В меню слева выберите:

Collaborators and teams
или

Manage access

Нажмите Invite a collaborator

Введите логин участника

Участники увидят приглашение в e-mail или уведомлениях GitHub.

4. Подключение репозитория на локальную машину
4.1. Получить ссылку на репозиторий
На GitHub → кнопка Code → вкладка HTTPS → скопировать ссылку.

4.2. Клонирование
bash
Копировать код
git clone https://github.com/OWNER/REPO.git
cd REPO
Теперь вы находитесь в папке, где есть скрытая директория .git, а значит, Git полностью активен.

5. Подготовка SSH-ключа (по желанию, но желательно)
5.1. Создать ключ
bash
Копировать код
ssh-keygen -t ed25519 -C "email_на_GitHub"
Жмите Enter везде.

5.2. Посмотреть ключ
bash
Копировать код
cat ~/.ssh/id_ed25519.pub
Скопируйте вывод.

5.3. Добавить в GitHub
GitHub → Settings → SSH and GPG keys → New SSH key → вставить → сохранить.

6. Структура веток проекта
Применяется стандартная схема:

css
Копировать код
main — стабильная версия
develop — основная ветка разработки
feature/<название-задачи> — отдельные ветки под задачи
7. Создание и настройка ветки develop
Если в репозитории её ещё нет:

bash
Копировать код
git checkout -b develop
git push -u origin develop
Теперь всем участникам нужно переключаться:

bash
Копировать код
git checkout develop
git pull
8. Создание feature-веток
Каждая задача выполняется в своей ветке.

Создать новую ветку:

bash
Копировать код
git checkout -b feature/имя-задачи
Примеры:

bash
Копировать код
git checkout -b feature/login-form
git checkout -b feature/api
git checkout -b feature/database-models
9. Рабочий цикл участника
9.1. Перед началом работы
Переключиться на develop и обновить его:

bash
Копировать код
git checkout develop
git pull
9.2. Создать ветку для задачи
bash
Копировать код
git checkout -b feature/имя-фичи
9.3. После внесения изменений
bash
Копировать код
git add .
git commit -m "Краткое описание изменений"
git push -u origin feature/имя-фичи
После первого пуша -u можно не писать:

bash
Копировать код
git push
10. Создание Pull Request (PR)
Когда задача выполнена:

Зайдите в GitHub → Pull Requests

Нажмите New Pull Request

Base branch — develop

Compare branch — ваша feature/...

Добавьте описание

Назначьте ревьюера

Отправьте PR

После проверки PR мёржат в develop.

11. Обновление своей feature-ветки (во избежание конфликтов)
bash
Копировать код
git checkout develop
git pull
git checkout feature/имя-фичи
git merge develop
Если появятся конфликты — Git укажет файлы, где они возникли.

12. Полезные команды Git
Показать статус:

bash
Копировать код
git status
Показать список веток:

bash
Копировать код
git branch
Показать удалённые ветки:

bash
Копировать код
git branch -r
Удалить локальную ветку:

bash
Копировать код
git branch -d feature/имя
Удалить ветку на GitHub:

bash
Копировать код
git push origin --delete feature/имя
Посмотреть историю коммитов:

bash
Копировать код
git log --oneline --graph --decorate --all
13. Правила команды
Не пушить в main

Все фичи — только через feature-ветки

Все изменения — через Pull Request

Каждый день обновлять develop

Писать понятные коммиты

Не мёржить свой PR без ревью

Следовать структуре проекта
